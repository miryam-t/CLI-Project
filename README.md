# CLI-Project
A CLI project in C# that allows you to combine multiple code files into one file. The tool works via the command line and allows you to create a unified file containing all the code files.
